Use `cargo test` to compile and run the test suite. This will run the document tests too.
To run ignored tests, use `cargo test -- --ignored`.

To output the documents from the crate, run `cargo doc`

